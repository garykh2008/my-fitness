# 本機開發用。複製成 .env.local 後填值（.env.local 不進版控）。
#
# 設計取向：所有 Supabase 存取都在 Next.js 伺服器端進行。
# 瀏覽器只跟 Next server 溝通，登入後的 JWT 存在 httpOnly cookie 裡，
# 由伺服器帶去打 PostgREST，權限由 Supabase Auth + RLS 把關。
#
# 因此：
#   1. 這些變數「沒有」NEXT_PUBLIC_ 前綴 —— 不會被編進前端 bundle，
#      也因此是執行期讀取，Docker 映像不用為了換設定重建。
#   2. 不需要 service_role key —— 少一把萬能鑰匙在外面跑。

# 自架 Supabase 的對外網址
SUPABASE_URL=https://supabase.garyhsieh-proj.com

# anon key。它本身不帶權限，真正的權限來自使用者登入後的 JWT + RLS。
SUPABASE_ANON_KEY=

# 資料表所在的 schema
SUPABASE_DB_SCHEMA=fitness
